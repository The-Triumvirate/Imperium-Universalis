incident_taoism_1 = {

	frame = 1
	variable_initial = 0

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { is_incident_active = yes }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 200
	}

	immediate_effect = {
		country_event = { id = taoism_events.19 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_2 = {

	frame = 2

	variable_initial = 4

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		is_institution_enabled = new_world_i
		num_of_ports = 1
		if = {
			limit = {
				normal_or_historical_nations = yes
				is_playing_custom_nation = no
			}
			custom_trigger_tooltip = {
				tooltip = japan_has_encountered_the_west_tooltip
				has_global_flag = japan_has_encountered_the_west
			}
		}
		if = {
			limit = {
				OR = {
					normal_or_historical_nations = no
					is_playing_custom_nation = yes
				}
			}
			custom_trigger_tooltip = {
				tooltip = encountered_other_continent_tooltip
				any_known_country = {
					NOT = { same_continent = ROOT }
				}
			}
		}
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { has_country_flag = active_incident_flag }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 200
		modifier = {
			factor = 0.7
			num_of_ports = 5
		}
	}

	immediate_effect = {
		country_event = { id = taoism_events.25 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_3 = {

	frame = 3

	variable_initial = 1

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		is_incident_happened = incident_nanban
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			is_incident_active = no
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 150
	}

	immediate_effect = {
		country_event = { id = taoism_events.32 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_4 = {

	frame = 4

	variable_initial = 0

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		NOT = { current_age = age_of_discovery }
		if = {
			limit = {
				normal_or_historical_nations = yes
			}
			custom_trigger_tooltip = {
				tooltip = japan_has_encountered_the_west_tooltip
				has_global_flag = japan_has_encountered_the_west
			}
		}
		custom_trigger_tooltip = {
			tooltip = encountered_christian_tooltip
			any_known_country = {
				religion_group = christian
			}
		}
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { has_country_flag = active_incident_flag }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 100
	}

	immediate_effect = {
		country_event = { id = taoism_events.38 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_5 = {
#Stance on central authority - Sankin Kotai, etc

	frame = 5

	variable_initial = 0

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		has_reform = daimyo
		OR = {
			current_age = age_of_reformation
			current_age = age_of_absolutism
			current_age = age_of_revolutions
		}
		OR = {
			overlord = {
				legitimacy = 90
			}
			overlord = {
				absolutism = 50
			}
			absolutism = 50
		}
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { has_country_flag = active_incident_flag }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 200
	}

	immediate_effect = {
		country_event = { id = taoism_events.50 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_6 = {

	frame = 6

	variable_initial = 0

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		NOT = { current_age = age_of_discovery }
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { has_country_flag = active_incident_flag }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 200
	}

	immediate_effect = {
		country_event = { id = taoism_events.13 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_7 = {

	frame = 7

	variable_initial = 0

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		num_of_ports = 1
		any_known_country = {
			num_of_ports = 1
			NOT = { religion = taoism }
		}
		any_neighbor_country = {
			exists = yes
		}
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { has_country_flag = active_incident_flag }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 200
	}

	immediate_effect = {
		country_event = { id = taoism_events.1 }
		set_country_flag = active_incident_flag
	}
}

incident_taoism_8 = {

	frame = 8

	variable_initial = 0

	potential = {
		has_dlc = "Mandate of Heaven"
		religion = taoism
	}

	trigger = {
		has_institution = global_trade
		num_of_cities = 2
		custom_trigger_tooltip = {
			tooltip = taoism_incident_recent_tooltip
			NOT = { has_country_flag = active_incident_flag }
			OR = {
				NOT = { has_country_flag = taoism_incident_recent }
				had_country_flag = { flag = taoism_incident_recent days = 10950 }
			}
		}
	}

	mean_time_to_happen = {
		months = 200
	}

	immediate_effect = {
		country_event = { id = taoism_events.7 }
		set_country_flag = active_incident_flag
	}
}