chinesecivilwar = { #Chaos of Wangzi Chao
	potential = {
		tag = ZHO
		#ruler_age = 20
		NOT = { has_any_disaster = yes }
		NOT = { exists = ZHG }
		has_country_flag = chongaiyouzi
		has_reform = free_city_zhou
		NOT = { has_country_flag = wangzichaozhiluan }
	}

	can_start = {
		is_at_war = no
		has_any_disaster = no
	}

	can_stop = {
		always = no
	}

	progress = {
		modifier = {
			factor = 25
			NOT = { stability = 0 }
			is_at_war = no
		}
		modifier = {
			factor = 10
			stability = 0
			NOT = { stability = 1 }
			is_at_war = no
		}
		modifier = {
			factor = 5
			stability = 1
			NOT = { stability = 2 }
			is_at_war = no
		}
		modifier = {
			factor = 2.5
			stability = 2
			NOT = { stability = 3 }
			is_at_war = no
		}
		modifier = {
			factor = 1
			stability = 3
			is_at_war = no
		}
	}

	can_end = {
		NOT = { num_of_rebel_armies = 1 }
		NOT = { exists = ZHG }
		hidden_trigger = { has_country_flag = zhou_civil_war_started_flag }
	}


	modifier = {
		stability_cost_modifier = 0.5
		legitimacy = -2
		global_unrest = 5
	}

	on_start = PaiXi.99
	on_end = PaiXi.101

	on_monthly = {
		events = {
		}

	}
}
