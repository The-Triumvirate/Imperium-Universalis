force_socii_peace_deal = {
	category = 6
	power_projection = vassalized_rival
	power_cost_base = 1.1
	prestige_base = 0.15
	ae_base = 1.0
	warscore_cost = {
		no_provinces = 30.0
		owner_keeps = 0.0
		returned_core = 0.0
		released_nation = 0.0
	}
	warscore_cap = 75
	is_make_subject = yes
	requires_is_allowed = no
	
	is_visible = {
		TAG = ROM
		has_reform = res_publica_reform
		is_subject = no
	}
	is_allowed = {
		FROM = {
			is_subject = no
			NOT = { num_of_subjects = 1 }
			capital_scope = { superregion = italia_superregion }
		}
	}
	effect = {
		custom_tooltip = force_socii_ct.1
		hidden_effect = {
			create_subject = {
				subject_type = socii_subject
				subject = FROM
			}
			add_prestige = 10
			add_mil_power = 50
		}
	}
	ai_weight = {
		export_to_variable = {
			variable_name = ai_value
			value = 60
		}
		limit = {
			prestige = 10
			dip_tech = 5
			government_rank = 2
			army_size = 10
			FROM = {
				NOT = { total_development = ROOT }
				NOT = { culture_group = hellenic }
			}
		}
	}
}
